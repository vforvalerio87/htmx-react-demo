export default function About() {
    return(
        <>
            <h1>About</h1>
            <p>This website uses server-rendered only React using the <code>renderToStaticMarkup</code> API from <code>react-dom/server</code> and htmx to fetch data</p>
        </>
    )
}